apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: 'php72-test-ssl'
  namespace: 'k8s-openresty-php'
spec:
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - 'php72-test.parallax.dev'
  commonName: 'php72-test.parallax.dev'
  dnsNames:
  - 'php72-test.parallax.dev'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: 'php72-test-ss'